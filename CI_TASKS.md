# Chat 项目 CI 改造任务清单

## 目标

将当前仓库改造成能够在干净的 Windows/Linux CI Runner 上完成代码检出、依赖安装、构建、测试、质量检查、集成验证、制品打包和版本发布的可重复工程。

本清单只描述后续改造任务，不代表这些任务已经完成。建议按任务编号顺序实施。

## 当前基线

- 仓库包含 Qt QWidget 客户端、GateServer、StatusServer、两套 ChatServer、Node.js VerifyServer 和一个占位 ResourceServer。
- Qt 客户端已有 CMake 和一个 CTest 测试目标；C++ 服务端主要依赖 Visual Studio `.vcxproj`。
- 仓库没有根级构建入口、CMake Presets、C++ 依赖清单或 CI 工作流。
- 服务端工程和协议生成脚本包含开发机绝对路径。
- C++ 服务依赖 Boost、gRPC、Protobuf、hiredis、jsoncpp、spdlog 和 MySQL Connector/C++，但没有统一、锁版本的依赖清单。
- 仓库提交了 6 个 MySQL Connector DLL。
- 多份配置文件包含非空邮箱、Redis 和 MySQL 凭据。
- 88 个服务端源码或协议文件不是有效 UTF-8，且存在多处仅大小写不同的 include 路径。
- 当前文本行尾没有 mixed 情况：常规文本使用 LF，两个 `.bat` 文件在工作区使用 CRLF；仍需由 CI 持续校验。
- ChatServer1/ChatServer2 高度重复，其他服务之间也存在多组完全相同的公共代码。
- Proto 文件及 C++ 生成物分散在多个服务目录，存在版本漂移风险。
- 服务端和 Node.js 服务没有自动化测试；客户端目前只有一个消息模型测试。
- 没有数据库初始化/迁移脚本、容器化集成环境、健康检查或完整制品规则。
- 根目录 `README.md` 为空。

## 完成定义

全部改造完成后应满足：

- 任意开发者或 CI Runner 仅根据仓库文件即可完成干净构建，不依赖开发机私有目录或全局用户配置。
- Windows 构建稳定通过；如果服务端声明支持 Linux，则 GCC/Clang 构建也必须通过。
- PR 自动执行格式、编码、密钥、构建和单元测试门禁。
- `develop` 分支自动执行 Release 构建、集成测试、覆盖率和制品生成。
- 发布标签自动执行完整回归、安全扫描、版本化打包和发布。
- 所有真实凭据均由 CI Secret 或运行环境注入，不进入 Git 仓库和构建制品。
- 构建、测试和打包命令在本地与 CI 中保持一致。

---

## 阶段 0：范围与平台决策

### CI-00 确认 CI 平台和支持矩阵

- [ ] 确认使用 GitHub Actions、GitLab CI、Jenkins 或其他平台。
- [ ] 确认 Qt 客户端支持的 Qt 主版本，目前建议明确为 Qt 6。
- [ ] 确认客户端是否只发布 Windows，还是同时发布 Linux/macOS。
- [ ] 确认 C++ 服务端是否要求正式支持 Linux。
- [ ] 确认 Node.js LTS 版本和最低版本。
- [ ] 确认 Debug、Release、编译器和架构矩阵。

验收标准：形成明确的平台矩阵，后续构建和测试不再声明未经验证的平台。

---

## 阶段 1：安全、编码和路径阻塞项（P0）

### CI-01 清理并轮换仓库中的凭据

- [ ] 识别所有已提交配置中的邮箱、Redis 和 MySQL 凭据。
- [ ] 立即轮换已经进入仓库的真实凭据。
- [ ] 将真实配置替换为不含秘密的 `config.example.ini`、`config.example.json` 等模板。
- [ ] 使用环境变量、命令行参数或 CI Secret 注入真实值。
- [ ] 确认运行日志不会输出配置值和敏感请求字段。
- [ ] 评估是否需要从 Git 历史中清理秘密；如需执行，单独制定历史重写和团队同步方案。
- [ ] 增加 secret scanning，并将其设为 PR 必需检查。

验收标准：当前版本和扫描范围内没有有效凭据，服务仍能通过环境变量正常启动。

### CI-02 统一源码字符编码

- [ ] 将检测到的 88 个非 UTF-8 服务端源码/协议文件转换为 UTF-8。
- [ ] 检查中文注释和字符串，避免仅转换字节但保留乱码。
- [ ] 为 MSVC、GCC 和 Clang 明确 UTF-8 源码编译选项。
- [ ] 增加仓库级 UTF-8 校验脚本并接入 CI。

验收标准：所有受管文本文件都是有效 UTF-8，Windows/Linux 编译器不再产生编码警告或乱码。

### CI-03 修复大小写敏感路径

- [ ] 统一 `Const.h/const.h`、`Data.h/data.h`、`Singleton.h/singleton.h` 等文件名和 include 写法。
- [ ] 在大小写敏感环境中检查所有本地 include。
- [ ] 增加 include 路径大小写校验。

验收标准：仓库在 Linux 文件系统上不存在因路径大小写导致的文件找不到错误。

### CI-04 固化行尾策略

- [ ] 提交并维护根目录 `.gitattributes` 和 `.editorconfig`。
- [ ] 常规源码、配置、CMake、JSON、JS、Proto 和文档统一使用 LF。
- [ ] Windows 批处理脚本按仓库策略使用 CRLF。
- [ ] 在 CI 中运行 `git diff --check` 和行尾检查。
- [ ] 验证干净 Windows/Linux checkout 后不会产生整文件行尾差异。

验收标准：仓库无 mixed 行尾，跨平台 checkout 后 `git status` 保持干净。

### CI-05 移除开发机绝对路径

- [ ] 删除 `.vcxproj` 中指向 `D:\VisualStudio\...` 的 Boost 和 MySQL 路径。
- [ ] 删除 `start.bat` 中指向 `D:\vcpkg\...` 的 protoc 和 gRPC 插件路径。
- [ ] 不再依赖 `$(UserRootDir)\Microsoft.Cpp.*.user.props` 中的个人包管理器集成。
- [ ] 由构建系统通过 target、toolchain 或包管理器发现依赖。
- [ ] 全仓扫描其他盘符、用户目录和固定工作目录。

验收标准：全新 Runner 无需创建开发机同名目录即可配置和构建。

---

## 阶段 2：依赖与统一构建系统（P0/P1）

### CI-06 建立锁版本的 C++ 依赖清单

- [ ] 选定 vcpkg manifest 或同等可重复依赖方案。
- [ ] 明确列出 Boost、gRPC、Protobuf、hiredis、jsoncpp、spdlog、MySQL Connector/C++ 等依赖。
- [ ] 固定依赖基线或版本，避免 Runner 每次解析到不同版本。
- [ ] 确认 Debug/Release 使用匹配的运行库和依赖变体。
- [ ] 删除仓库中的 MySQL Connector DLL，并改由包管理或打包阶段提供。
- [ ] 记录第三方许可证和依赖来源。

验收标准：依赖只由受管清单解析，相同 commit 能得到一致依赖图。

### CI-07 建立根级 CMake 构建入口

- [ ] 在根目录建立统一 `CMakeLists.txt`。
- [ ] 将 client、gate、status、chat、resource 定义为清晰的独立 target。
- [ ] 增加按需开关，例如 `BUILD_CLIENT`、`BUILD_SERVERS`、`BUILD_TESTING`。
- [ ] 使用 target 级 include、compile definitions 和 link dependencies，避免全局路径污染。
- [ ] 统一输出目录和安装目录。
- [ ] 保证所有 C++17 要求由构建目标显式声明。

验收标准：从仓库根目录运行标准 CMake 命令即可构建所有选定目标。

### CI-08 增加 CMake Presets

- [ ] 增加 Windows MSVC Debug preset。
- [ ] 增加 Windows MSVC Release preset。
- [ ] 如果支持 Linux，增加 GCC 和/或 Clang preset。
- [ ] 将包管理器 toolchain、构建目录和测试选项写入 preset。
- [ ] 本地文档和 CI 只能调用 preset，不维护另一套隐藏构建参数。

验收标准：开发者与 CI 使用相同的 `cmake --preset`、`cmake --build --preset` 命令。

### CI-09 修正 Qt 客户端构建声明

- [ ] 明确只支持 Qt 6，或修复 Qt 5 分支使其包含完整源文件。
- [ ] 避免在 `qt_add_executable` 中手工维护容易遗漏的超长源文件列表，评估按模块拆分 target。
- [ ] 保持 Qt Widgets、Network、spdlog 等依赖为 target 级声明。
- [ ] 为 `config.ini`、`static/`、Qt plugins 和运行库增加正式 install/package 规则。
- [ ] 保证测试在无显示器 Runner 上使用稳定的 headless Qt 平台插件。

验收标准：声明支持的每个 Qt 平台均可配置、编译、测试和安装。

### CI-10 整理 Node.js 工程入口

- [ ] 在 `package.json` 中增加 `engines.node` 和 `packageManager`。
- [ ] 增加标准 `start`、`test`、`lint`、`format:check` 或 `check` 脚本。
- [ ] CI 使用 `npm ci`，不使用会修改 lock 文件的安装方式。
- [ ] 评估将 lock 文件中的固定区域镜像地址改成官方地址或 CI 可配置镜像。
- [ ] 验证 `package.json` 与 `package-lock.json` 完全同步。

验收标准：指定 Node/npm 版本下，干净安装和检查命令稳定通过。

---

## 阶段 3：代码结构重构（P1）

### CI-11 合并 ChatServer1 和 ChatServer2

- [ ] 对两套代码的差异进行逐文件分类：实例配置差异、真实业务差异、意外漂移。
- [ ] 只保留一个 ChatServer 源码目标。
- [ ] 将服务器名称、监听端口、RPC 端口和 peer 信息配置化。
- [ ] 在集成测试或部署中使用同一二进制启动两个实例。
- [ ] 删除重复工程、重复生成物和重复依赖配置。

依赖：CI-02、CI-03、CI-07。

验收标准：ChatServer 只编译一次，两实例行为由配置决定。

### CI-12 抽取服务端公共库

- [ ] 将 ConfigMgr、LogMgr、Redis、MySQL、DistLock、Singleton 等公共实现分类。
- [ ] 建立 `libs/common` 或更细粒度的公共 target。
- [ ] 公共库只暴露必要头文件，避免服务之间互相 include 源目录。
- [ ] 为外部系统定义接口，使业务代码可使用 mock/fake 测试。
- [ ] 删除 Gate、Status、Chat 之间的完全重复文件。

依赖：CI-07；建议在基础单元测试建立后完成最终替换。

验收标准：公共实现只有一个来源，修改一次即可覆盖全部服务。

### CI-13 集中管理 Proto 和生成物

- [ ] 在根目录建立唯一 `proto/` 协议目录。
- [ ] 合并或拆分当前多份 `message.proto`，明确服务边界和版本策略。
- [ ] 由 CMake 自动调用锁定版本的 protoc 和 gRPC 插件。
- [ ] 决定生成物是否提交；无论采用哪种策略，都要提供“生成物最新”校验。
- [ ] 增加破坏性协议变更检测或兼容性检查。
- [ ] Node.js 与 C++ 服务使用同一协议来源。

依赖：CI-06、CI-07。

验收标准：协议只有一个权威来源，所有语言绑定由统一流程生成或验证。

### CI-14 规范运行时配置

- [ ] 为所有服务提供一致的 `--config` 参数或环境变量约定。
- [ ] 不再根据进程当前工作目录隐式查找配置。
- [ ] 客户端、C++ 服务和 Node.js 服务采用一致的覆盖优先级：命令行、环境变量、配置文件、默认值。
- [ ] 增加配置 schema 和启动前校验，包括必填字段、端口范围、地址格式和日志目录。
- [ ] 修复 GateServer 配置写入 `Password`、代码读取 `Passwd` 的不一致。
- [ ] 将 VerifyServer 固定监听端口改为配置项。
- [ ] 配置错误必须输出明确错误并返回非零退出码，但不得输出秘密值。

验收标准：任意工作目录启动结果一致，CI 可完全通过临时配置和 Secret 驱动服务。

### CI-15 确认 ResourceServer 生命周期

- [ ] 确认该目录是计划中的正式服务还是 Visual Studio 占位项目。
- [ ] 如果不再需要，从正式构建和 CI 范围中移除。
- [ ] 如果需要，定义功能、配置、依赖、测试和制品规则。

验收标准：CI 中不存在用途不明确的构建目标。

### CI-16 整理推荐目录结构

- [ ] 将 GUI 客户端放入清晰的 `apps/client` 或等价目录。
- [ ] 将 Gate、Status、Chat、Verify 等服务统一放入 `services/`。
- [ ] 将公共 C++ 代码放入 `libs/`。
- [ ] 将共享协议放入 `proto/`。
- [ ] 将单元、组件、集成和 E2E 测试按层级放入 `tests/`。
- [ ] 将容器、配置模板和部署资源放入 `deploy/`。
- [ ] 在移动文件时保留 Git rename 可追踪性并分小批提交。

目标结构示例：

```text
Chat/
├─ CMakeLists.txt
├─ CMakePresets.json
├─ vcpkg.json
├─ apps/client/
├─ services/
│  ├─ gate/
│  ├─ status/
│  ├─ chat/
│  ├─ verify/
│  └─ resource/
├─ libs/common/
├─ proto/
├─ tests/
├─ deploy/
│  ├─ docker/
│  └─ config/
└─ .github/workflows/ 或目标 CI 平台目录
```

---

## 阶段 4：测试和集成环境（P1）

### CI-17 建立测试分层和统一入口

- [ ] 所有 C++ 测试通过 CTest 注册。
- [ ] Node.js 测试通过 `npm test` 执行。
- [ ] 区分 unit、component、integration、smoke 和 e2e 标签。
- [ ] 为每层测试设置合理超时、并行策略和失败日志。
- [ ] 测试不得依赖开发者主机上的固定端口或数据。

验收标准：根级命令可以按层级运行测试，失败时 CI 能定位具体测试。

### CI-18 补齐单元测试

- [ ] 扩充客户端消息模型和视图模型测试。
- [ ] 测试配置解析、缺失字段和非法字段。
- [ ] 测试协议转换和序列化边界。
- [ ] 测试 Gate、Status、Chat 的核心业务处理器。
- [ ] 测试 VerifyServer 的验证码生成、Redis 访问和邮件适配逻辑。
- [ ] 使用 mock/fake 隔离数据库、Redis、SMTP 和远程 gRPC 服务。

依赖：CI-12、CI-14。

验收标准：核心业务失败路径和边界条件都有可重复单元测试。

### CI-19 提供数据库迁移和测试数据

- [ ] 将当前服务依赖的 MySQL schema 写成版本化迁移脚本。
- [ ] 增加最小测试种子数据。
- [ ] 确保迁移可在空数据库中自动执行。
- [ ] 为迁移本身增加验证或回滚策略。

验收标准：CI 无需人工准备数据库即可运行组件和 E2E 测试。

### CI-20 建立容器化集成环境

- [ ] 使用 Docker Compose 或 CI Services 启动 MySQL 和 Redis。
- [ ] 为 SMTP 使用 mock server、测试 SMTP 或禁用适配器。
- [ ] 为每个 Job 创建隔离数据库和 Redis namespace。
- [ ] 使用动态端口或 Job 唯一端口，允许并行执行。
- [ ] 测试结束后可靠回收服务和临时数据。

依赖：CI-14、CI-19。

验收标准：一条本地命令和一个 CI Job 都能启动完整依赖环境并运行集成测试。

### CI-21 增加健康检查和可控生命周期

- [ ] 为 Gate、Status、Chat 和 Verify 服务增加 readiness/liveness 检查。
- [ ] 区分“进程已启动”和“依赖已连接、可以服务”。
- [ ] 支持 SIGINT/SIGTERM 或 Windows 等价停止流程。
- [ ] 统一启动失败退出码和超时。
- [ ] 确认后台线程、连接池、Redis 和 gRPC Server 能可靠关闭。

验收标准：CI 能等待服务就绪、判断失败，并在超时后安全终止全部进程。

### CI-22 增加冒烟和端到端测试

- [ ] 启动 VerifyServer、StatusServer、两个 ChatServer 实例和 GateServer。
- [ ] 验证各服务健康状态和 gRPC 连接。
- [ ] 覆盖注册/验证码替身流程。
- [ ] 覆盖登录和 ChatServer 分配流程。
- [ ] 覆盖建立会话和发送消息的最小主路径。
- [ ] 覆盖至少一个依赖不可用或配置错误场景。

依赖：CI-17 至 CI-21。

验收标准：develop 分支能够自动验证完整服务链路。

### CI-23 建立覆盖率策略

- [ ] 生成 C++ 和 Node.js 覆盖率报告。
- [ ] 排除 vendored、生成代码和 UI 自动生成代码。
- [ ] 初期只阻止覆盖率下降，稳定后再提高最低阈值。
- [ ] 将报告作为 CI artifact 或 PR 注释提供。

验收标准：每次合并都能看到可靠覆盖率变化，并防止核心代码覆盖率持续下降。

---

## 阶段 5：质量和安全门禁（P2）

### CI-24 建立统一格式化规则

- [ ] 增加项目级 `.clang-format`，不要只依赖 vendored spdlog 的规则。
- [ ] Node.js 增加 Prettier 或等价格式检查。
- [ ] CMake、JSON、YAML 和 Markdown 选择适用的格式校验工具。
- [ ] CI 仅检查格式，不在 Runner 上自动提交修改。

验收标准：格式检查可在本地一键执行，并作为 PR 必需检查。

### CI-25 建立静态分析和编译告警策略

- [ ] 增加项目级 `.clang-tidy`。
- [ ] 启用 MSVC、GCC、Clang 的合理警告等级。
- [ ] 先记录历史告警基线，再逐步启用 warnings-as-errors。
- [ ] 为高风险目标增加 ASan/UBSan 或 MSVC Sanitizer Job。
- [ ] Node.js 增加 ESLint。

验收标准：新增代码不能引入新的高严重度静态问题或编译告警。

### CI-26 增加依赖和供应链检查

- [ ] 扫描 npm 和 C++ 依赖漏洞。
- [ ] 增加依赖自动更新工具并限制自动合并范围。
- [ ] 生成 SBOM。
- [ ] 生成第三方许可证清单。
- [ ] 校验下载依赖的完整性和可信来源。

验收标准：高危依赖漏洞阻止发布，制品可以追溯其依赖组成。

### CI-27 增加代码与制品安全扫描

- [ ] 增加 SAST。
- [ ] 增加 secret scanning。
- [ ] 如果构建容器镜像，增加镜像漏洞扫描。
- [ ] 对发布包执行恶意文件或安全策略扫描。
- [ ] 明确告警抑制流程和过期时间，避免永久忽略。

验收标准：高严重度安全问题进入明确的阻断或审批流程。

---

## 阶段 6：安装、打包和发布（P2）

### CI-28 完善客户端安装和打包

- [ ] 使用 CMake install 安装客户端可执行文件、配置模板和静态资源。
- [ ] Windows 使用 `windeployqt` 收集 Qt DLL、platform plugins 和 image plugins。
- [ ] 验证安装包不依赖构建目录中的文件。
- [ ] 在干净虚拟机或 Runner 上执行启动冒烟测试。
- [ ] 明确是否生成 ZIP、安装程序或其他格式。

验收标准：下载 CI 制品后无需安装开发工具即可运行客户端。

### CI-29 完善服务端制品

- [ ] 每个服务制品包含二进制、配置模板、许可证和必要运行库。
- [ ] 制品不得包含真实配置、日志、缓存或测试数据。
- [ ] 如果支持 Linux 部署，为服务建立最小化容器镜像。
- [ ] 为容器设置非 root 用户、健康检查和固定入口点。

验收标准：制品可部署到干净目标环境，并能通过启动冒烟测试。

### CI-30 建立版本和制品命名规则

- [ ] 明确语义化版本或项目版本规则。
- [ ] 普通分支制品包含 commit SHA。
- [ ] 标签制品使用标签版本并校验与项目版本一致。
- [ ] 生成 SHA-256 校验和。
- [ ] 调试符号与运行包分开保存。
- [ ] 明确制品保留周期。

验收标准：任何发布包都可以追溯到唯一 commit、构建配置和依赖清单。

### CI-31 建立发布流程

- [ ] 只有通过完整测试和安全门禁的标签可以发布。
- [ ] 配置发布环境审批和最小权限凭据。
- [ ] 生成 release notes 或 changelog。
- [ ] 如有需要，对 Windows 可执行文件/安装包进行代码签名。
- [ ] 定义失败发布和回滚流程。

验收标准：发布过程自动、可审计，并且不能绕过必要门禁。

---

## 阶段 7：CI 工作流落地（P2）

### CI-32 PR 快速验证工作流

- [ ] 检查 UTF-8、行尾、大小写、格式和秘密。
- [ ] 校验依赖清单、lock 文件和 Proto 生成状态。
- [ ] 执行 Debug 构建。
- [ ] 执行 C++/Node.js 单元测试。
- [ ] 对纯文档修改使用安全的路径过滤，避免漏掉构建相关文档或配置。
- [ ] 配置并发取消，推送新 commit 后取消旧 Job。

目标：尽量在可接受时间内给 PR 提供反馈。

### CI-33 develop 完整验证工作流

- [ ] 执行 Windows/Linux 支持矩阵。
- [ ] 执行 Release 构建。
- [ ] 执行组件、集成、冒烟和 E2E 测试。
- [ ] 生成覆盖率、静态分析和安全报告。
- [ ] 生成并上传可运行制品。

验收标准：`develop` 始终保持可构建、可测试、可交付状态。

### CI-34 标签发布工作流

- [ ] 验证标签和项目版本。
- [ ] 执行完整回归与安全扫描。
- [ ] 构建最终 Release 制品。
- [ ] 生成校验和、SBOM、许可证和发布说明。
- [ ] 完成签名、上传和发布审批。

验收标准：标签发布不依赖开发者本地手工打包。

### CI-35 配置可靠缓存

- [ ] 缓存 npm 下载目录，不缓存不可验证的工作区 `node_modules`。
- [ ] 缓存 C++ 包管理器下载和编译结果。
- [ ] 缓存键包含 OS、架构、编译器、Qt/Node 版本和依赖 lock/hash。
- [ ] 缓存失败时构建仍可从零完成。
- [ ] 定期验证无缓存构建，防止隐藏依赖。

验收标准：缓存只优化速度，不影响构建正确性或可重复性。

### CI-36 配置分支保护和权限

- [ ] 为 `develop` 设置必需检查。
- [ ] 禁止未通过 CI 的提交直接合并。
- [ ] 限制发布 Secret 仅在受保护环境使用。
- [ ] 默认工作流 token 使用只读最小权限，需要写权限的 Job 单独声明。
- [ ] 明确管理员绕过和紧急修复流程。

验收标准：CI 门禁不能被普通合并路径绕过，发布凭据遵循最小权限原则。

---

## 阶段 8：文档和维护（P2）

### CI-37 补齐根 README

- [ ] 描述系统架构和各服务职责。
- [ ] 列出受支持的平台、编译器、Qt、Node 和外部依赖版本。
- [ ] 提供干净构建、测试、格式化、静态分析和打包命令。
- [ ] 描述配置覆盖顺序和 Secret 注入方式，但不提供真实秘密。
- [ ] 描述本地集成环境启动和关闭方法。
- [ ] 描述常见 CI 失败的排查方式。

验收标准：新开发者仅根据 README 即可复现 CI 的主要流程。

### CI-38 建立维护规则

- [ ] 指定依赖更新周期。
- [ ] 指定基础 Runner、Qt、Node、编译器升级策略。
- [ ] 定期运行完整无缓存构建。
- [ ] 定期验证 Secret、SAST、SCA 和镜像扫描规则。
- [ ] 记录 flaky test，禁止通过无限重试掩盖真实问题。
- [ ] 定期清理过期制品和缓存。

验收标准：CI 不只是首次可用，而是可以长期稳定维护。

---

## 建议里程碑

### 里程碑 M1：干净构建

- [ ] 完成 CI-00 至 CI-10。
- [ ] Windows Runner 可完成全部受支持目标的干净构建。
- [ ] 如果声明 Linux 支持，Linux Runner 同样通过。

### 里程碑 M2：结构统一

- [ ] 完成 CI-11 至 CI-16。
- [ ] ChatServer 合并、公共库和 Proto 单一来源落地。

### 里程碑 M3：可自动验证

- [ ] 完成 CI-17 至 CI-23。
- [ ] 单元、集成和最小 E2E 测试进入 `develop` 门禁。

### 里程碑 M4：质量与安全门禁

- [ ] 完成 CI-24 至 CI-27。
- [ ] 格式、静态分析、秘密和高危漏洞均可自动阻断。

### 里程碑 M5：可发布

- [ ] 完成 CI-28 至 CI-38。
- [ ] PR、develop 和 tag 三层流水线完整运行。
- [ ] 发布制品可追溯、可安装、可扫描、可回滚。

## 执行注意事项

- 每项结构重构应使用独立、小范围提交，避免同时移动文件和修改业务逻辑。
- 在合并 ChatServer 或抽公共库前，优先建立最小行为测试，降低重构回归风险。
- 不要在同一提交中进行大规模编码转换和业务修改。
- 不要为了让 CI 变绿而关闭测试、忽略退出码或永久抑制安全告警。
- CI 脚本应调用仓库提供的标准构建/测试入口，避免将关键逻辑只写在 CI YAML 中。
