---
context: default
phase: 1-windows-local-baseline
task: 3
total_tasks: 5
status: in_progress
last_updated: 2026-08-12T17:32:44.794Z
---

# BLOCKING CONSTRAINTS - Read Before Anything Else

> These constraints were discovered through actual failures.

- [ ] Keep vcpkg build trees and package staging on C: and retain per-port cleanup. A prior restore filled D: and caused Git index writes to fail.
- [ ] Treat GitHub downloads through the local proxy as intermittent. Never accept a manually downloaded archive until its SHA512 exactly matches the pinned vcpkg portfile.
- [ ] Use `D:\vcpkg\test-vcpkg` as `VCPKG_ROOT`. Do not switch back to the shallow old checkout; it cannot resolve the manifest baseline.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Temporary trees on D: | gRPC caused `D:\vcpkg\test-vcpkg\buildtrees` and `packages` to consume about 35 GB and filled D:. | blocking | Set `VCPKG_BUILDTREES_ROOT=C:\vcpkg-chat-temp\buildtrees` and `VCPKG_PACKAGES_ROOT=C:\vcpkg-chat-temp\packages`; keep both clean-after-build flags in the script. |
| Blind retry of GitHub archive downloads | Proxy SSL error 35 repeatedly stopped vcpkg on individual Boost archives. | blocking | For each failed archive, read REF/SHA512 from its generated versioned portfile, use the GitHub codeload URL, verify SHA512, and only then resume. |
| Replacing user-owned files | `tests/auto/` was already untracked before this work. | advisory | Preserve it and exclude it from edits/commits for phase 1. |

<current_state>

Phase 1 is paused during the pinned server dependency restore. All build processes
have been stopped. The most recent run began with 45 packages left, installed
`boost-xpressive:x64-windows`, and then failed downloading
`boostorg-tti-boost-1.90.0.tar.gz` with curl error 35. Approximately 44 packages
remain; rerunning is incremental.

</current_state>

<completed_work>

- Moved the complete Gitee clone from `D:\test-vcpkg` to
  `D:\vcpkg\test-vcpkg`; preserved `D:\vcpkg\vcpkg` unchanged.
- Checked out manifest baseline `fc3be1ebea7eaeb3071fe716ac65713af1f3a146`
  and reused the matching old `vcpkg.exe` (tool commit
  `4b3e4c276b5b87a649e66341e11553e8c577459c`).
- Added the `x64-windows-chat` mixed triplet: dynamic CRT and libraries by
  default, static only for `mysql-connector-cpp` and `libmysql` because JDBC is
  static-only at the pinned registry version.
- Centralized manifest integration, `/MDd`/`/MD`, app-local deployment and
  actionable `VCPKG_ROOT` validation in `Directory.Build.*`.
- Removed personal `D:\VisualStudio` dependency paths and the GateServer
  wildcard DLL copy from four project files; updated protobuf helper scripts.
- Added `scripts/windows-local.ps1` and `WINDOWS_BUILD.md`.
- Verified toolchain Check, Qt 6.5.3 MinGW client Debug build/CTest (1/1), and
  npm lockfile dry-run earlier in the session.
- Restored gRPC, protobuf, OpenSSL, hiredis, jsoncpp and most Boost packages.
  gRPC built successfully after its source archive was manually cached and
  SHA512-verified.
- Downloaded and SHA512-verified the missing Boost xpressive archive; its host
  package installed successfully on the next run.

</completed_work>

<remaining_work>

- Finish dependency restore (about 44 packages remain). Current failure is the
  missing Boost TTI archive only.
- Confirm all direct manifest dependencies and MySQL JDBC headers/libraries are
  installed under `vcpkg_installed\x64-windows-chat`.
- Build GateServer, StatusServer, ChatServer1 and ChatServer2 in Debug and
  Release with the local script; fix repository build errors if found.
- Verify each server output receives its runtime DLLs app-locally.
- Rebuild/recheck the Qt client in Debug and Release, run CTest, run real
  `npm ci` for VarifyServer, then do final diff/static checks and update docs.

</remaining_work>

<decisions_made>

- Dynamic linkage remains the Windows baseline; only MySQL JDBC/libmysql use
  static libraries due to the port's feature constraint. CRT remains dynamic.
- Each server will ultimately be an independent app-local deployment directory,
  not dependent on global DLLs or sibling service folders.
- The complete vcpkg checkout is the registry/tool root; the old checkout is
  retained for its downloads, installed resources and binary cache reuse.
- `VCPKG_DOWNLOADS` points to `D:\vcpkg\vcpkg\downloads` and
  `VCPKG_BINARY_SOURCES` points to
  `C:\Users\Lenovo\AppData\Local\vcpkg\archives`.

</decisions_made>

<blockers>

- Current external blocker: intermittent local-proxy SSL handshake failures on
  GitHub archive URLs. The established workaround is exact codeload download
  plus pinned SHA512 validation. No user action is currently required.

</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` - exact machine-readable state and next action.
2. `scripts/windows-local.ps1` - current restore/build commands and temp cleanup.
3. `triplets/x64-windows-chat.cmake` - mixed linkage rule.
4. `WINDOWS_BUILD.md` - intended phase-one local workflow.
5. `Directory.Build.props` and `Directory.Build.targets` - MSBuild/vcpkg policy.

## Infrastructure State

- No active `vcpkg`, `cmake`, `ninja`, `cl`, or `link` processes at pause.
- `D:\vcpkg\test-vcpkg`: complete checkout at the manifest baseline; usable.
- `D:\vcpkg\vcpkg`: old shallow installation, preserved and used for caches.
- `C:\vcpkg-chat-temp`: regenerable build/package staging; currently cleaned.
- Free space at pause: C: about 41.23 GB; D: about 7.84 GB.
- Proxy used successfully with curl: `http://127.0.0.1:7890`.
- Worktree has intentional uncommitted phase-one changes. `tests/auto/` is
  pre-existing user-owned untracked content and must not be touched.

<context>

The dependency restore is expensive but safely incremental. Do not start from
scratch or delete `vcpkg_installed`. The next failed GitHub archive may require
the same verified codeload workaround; this is expected. Disk pressure is now
controlled by putting temporary trees on C: and cleaning after each port.

</context>

<next_action>

Start by confirming no build process is active. Read
`C:\vcpkg-chat-temp\buildtrees\versioning_\versions\boost-tti\588fcdc557083ba15d53f09526ae7c9cbf4a1a9a\portfile.cmake`
for the exact SHA512, download the matching Boost 1.90.0 TTI archive from
`codeload.github.com` to
`D:\vcpkg\vcpkg\downloads\boostorg-tti-boost-1.90.0.tar.gz`, verify SHA512,
then rerun `RestoreServers` with the saved proxy/cache/temp environment.

</next_action>
